const Lib = require('../src/lib');
const sdk = require('matrix-js-sdk');
const stub = require('sinon');

// const sdk = {

// }
describe('test leave', () => {
    it('expect work correct', () => {

    });
});
